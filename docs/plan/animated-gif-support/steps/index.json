{
  "project": "diary",
  "phase": "animated-gif-support",
  "steps": [
    {
      "step": 0,
      "name": "gif-frame-decoder",
      "status": "completed",
      "started_at": "2026-07-18T15:32:53+0900",
      "summary": "공개 시그니처: decodeGifFrames(blob: Blob) -> Promise<{ frames: HTMLCanvasElement[], delays: number[] }> (src/fabric/gifFrameDecoder.js). gifuct-js의 parseGIF/decompressFrames(buildImagePatches=true)로 얻은 프레임별 patch(dims, disposalType)를 캔버스 하나(compositeCanvas)에 순서대로 누적해 매 프레임을 '완전 합성된' 새 canvas로 clone해 반환 — disposalType 2(restore-to-background)는 clearRect, 3(restore-to-previous)는 직전 스냅샷 복원, 나머지는 그대로 둔다. delay는 gifuct-js가 이미 ms 단위로 변환해 주므로 그대로 쓰되 MIN_FRAME_DELAY_MS=20으로 하한을 둔다. header.signature !== 'GIF'거나 프레임이 0개면 Error를 던진다(호출부 책임 없음, 조용히 무시 안 함). 테스트(gifFrameDecoder.test.js)는 실제 파일을 추가하지 않고 손수 만든 base64 GIF 바이너리(disposal method 2종 포함)를 인라인해 픽셀 단위로 합성 결과를 검증한다. 다음 step(animated-gif-object)은 이 함수를 호출해 frames/delays를 FabricImage 상속 오브젝트에 넣으면 된다 — Fabric은 이 파일에서 import하지 않았다.",
      "completed_at": "2026-07-18T15:42:01+0900"
    },
    {
      "step": 1,
      "name": "animated-gif-object",
      "status": "completed",
      "started_at": "2026-07-18T15:42:02+0900",
      "summary": "공개 시그니처: class AnimatedGif extends FabricImage (src/fabric/AnimatedGif.js), static type='AnimatedGif', static customProperties=['assetId','frameDelays','currentFrameIndex']. constructor(element, options)는 options.frames(HTMLCanvasElement[], 기본 [element])/frameDelays/currentFrameIndex(기본 0)를 세팅. advanceFrame()은 currentFrameIndex를 순환시키고 this._element를 frames[idx]로 교체 — frames.length<=1이면 no-op. _render/render/toObject는 오버라이드하지 않음(customProperties 메커니즘에 위임). static async fromObject(object, options)는 assetId로 getAsset() 조회 후 decodeGifFrames(record.blob)로 재디코딩해 new AnimatedGif(frames[0], {...object, frames, frameDelays: delays})를 반환하고, getAsset이 null이거나 디코딩 실패 시 FabricImage.fromObject(object에서 assetId/frameDelays/currentFrameIndex 제외한 나머지, options)로 폴백(에러를 던지지 않음). 파일 하단에서 classRegistry.setClass(AnimatedGif) 호출. 주의: FabricImage.toObject를 오버라이드하지 않으므로 직렬화 시 상속받은 src 필드(getSrc() 결과, 현재 프레임의 dataURL)가 함께 포함된다 — 이 값이 폴백 경로에서 FabricImage.fromObject가 이미지를 복원하는 데 쓰인다. 다음 step(shared-render-loop)은 advanceFrame()을 호출해 프레임을 넘기고 canvas.renderAll()을 호출하는 requestAnimationFrame 루프를 만들면 된다 — 이 파일은 스케줄링을 하지 않는다.",
      "completed_at": "2026-07-18T15:47:13+0900"
    },
    {
      "step": 2,
      "name": "shared-render-loop",
      "status": "completed",
      "started_at": "2026-07-18T15:47:14+0900",
      "summary": "공개 시그니처: createSharedGifRenderLoop(fabricCanvas) -> { register(obj), unregister(obj), start(), stop() } (src/fabric/sharedGifRenderLoop.js). 내부 Set+WeakMap으로 등록 오브젝트별 마지막 프레임전환 시각을 추적, 매 rAF 틱마다 obj.frameDelays[obj.currentFrameIndex]가 지난 오브젝트만 advanceFrame() 호출하고, 이번 틱에 하나라도 갱신됐을 때만 fabricCanvas.renderAll()을 최대 1회 호출(ADR-4). start()는 rafId가 이미 있으면 no-op, stop()은 cancelAnimationFrame 후 rafId를 null로 리셋. useFabricCanvas.js는 fabricCanvas 생성 직후 이 루프를 만들어 start()하고, object:added/object:removed에 별도 리스너를 달아 target instanceof AnimatedGif일 때만 register/unregister하며(fabric의 loadFromJSON도 내부적으로 canvas.add()를 거쳐 object:added가 발생하므로 재로드된 AnimatedGif도 이 리스너 하나로 자동 등록됨 — 별도 처리 불필요, 소스 확인함), effect cleanup에서 gifRenderLoop.stop()을 fabricCanvas.dispose() 이전에 동기 호출. 다음 step(gif-placement-integration)은 canvasAssetPlacement.js/ImageUploadButton에서 AnimatedGif 인스턴스를 만들어 canvas.add()하기만 하면 이 루프에 자동 register된다 — 이 step은 스케줄링 배선만 하고 GIF 판별/배치 로직은 건드리지 않았다.",
      "completed_at": "2026-07-18T15:58:33+0900"
    },
    {
      "step": 3,
      "name": "gif-placement-integration",
      "status": "completed",
      "started_at": "2026-07-18T15:58:33+0900",
      "summary": "공개 시그니처: createImageOrAnimatedGifObject({ blob, assetId, mimeType }) -> Promise<FabricImage | AnimatedGif> (src/fabric/placeImageOrGif.js) — 이게 GIF 판별/분기의 유일한 위치다. mimeType(없으면 blob.type)이 image/gif가 아니거나 decodeGifFrames 결과 frames.length===1이면 FabricImage.fromURL(objectURL) 정지 이미지를, frames.length>=2면 new AnimatedGif(frames[0], { assetId, frames, frameDelays })를 반환한다. 스케일 다운/중앙배치/canvas.add()는 안 함(호출부 책임). canvasAssetPlacement.js의 addImageAssetToCanvas와 ImageUploadButton.jsx의 addImageToCanvas가 둘 다 이 함수 하나만 호출하도록 바꿈. ImageUploadButton.jsx는 ADR-5대로 FileReader.readAsDataURL을 걷어내고 saveAsset({type:'image',...,blob:file})로 IndexedDB에 저장 후 반환된 id를 assetId로 써서 이 함수를 호출하는 방식으로 전환(기존처럼 input reset은 saveAsset을 await하지 않고 동기로 즉시 수행 — 같은 파일 재선택 허용 동작 유지). 신규/수정 테스트 파일: src/hooks/canvasAssetPlacement.test.js(GIF/정적GIF/PNG 3케이스 추가), src/components/ImageUploadButton/ImageUploadButton.test.jsx(신규, saveAsset 저장·스케일다운·GIF/PNG 분기 검증). npm run lint && npm run build && npm run test(144 passed) 전부 통과 확인함. 다음 step(gif-persistence)은 useFabricCanvas.js의 resolveObjectAssetReference에 AnimatedGif(type==='AnimatedGif') 통과 분기를 추가하고 저장/재로드 흐름을 검증하면 된다 — 이 step은 그 부분을 건드리지 않았다.",
      "completed_at": "2026-07-18T16:06:06+0900"
    },
    {
      "step": 4,
      "name": "gif-persistence",
      "status": "completed",
      "started_at": "2026-07-18T16:06:06+0900",
      "summary": "resolveObjectAssetReference(src/hooks/useFabricCanvas.js)에 object.type==='AnimatedGif' 통과 분기 추가 — AnimatedGif.fromObject가 assetId로 직접 재조회·재디코딩하므로 여기서 채우는 src는 무의미하고, getAsset 중복 호출과 해제 안 되는 objectURL만 남기는 걸 확인해서 고쳤다. 렌더 루프 등록은 step2에서 이미 object:added 리스너로 배선돼 있었고(loadFromJSON도 내부에서 canvas.add()를 거쳐 object:added가 발생), 이 step은 그걸 실제 저장→재로드 통합 테스트로 고정만 했다 — 프로덕션 코드 추가 배선 불필요했음. toObject(EXTRA_SERIALIZED_PROPS) 직렬화 결과에 frames 키가 없음을 JSON.stringify로 직접 검증(ADR-3 최종 확인). 신규 테스트는 src/hooks/useFabricCanvas.test.js에 3개 추가(resolveCanvasAssetReferences의 AnimatedGif 통과 분기, 직렬화에 frames 미포함, 직렬화→새 useFabricCanvas 인스턴스 로드→AnimatedGif 복원+공유 렌더 루프 재생 확인) — 마지막 통합 테스트에서 React act() 콜백 안에 root.render와 비동기 onLoaded 대기를 같이 넣으면 useEffect의 passive effect가 아예 플러시되지 않아 영원히 멈추는 함정을 발견해, act(render) 완료 후 별도로 onLoaded Promise를 await하는 두 단계로 분리해 해결(기존 lifecycle 테스트와 동일 패턴). npm run lint && npm run build && npm run test(147 passed) 전부 통과. 이 phase의 마지막 step — 다음은 사람에게 최종 결과 설명.",
      "completed_at": "2026-07-18T16:19:16+0900"
    }
  ],
  "created_at": "2026-07-18T15:32:53+0900"
}